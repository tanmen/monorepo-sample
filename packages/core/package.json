{
  "name": "@tanmen/core",
  "version": "0.0.0",
  "main": "index.js",
  "author": "tanmen",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "prepublish": "yarn build && cp package.json dist && cp ../../LICENSE dist"
  },
  "publishConfig": {
    "directory": "dist"
  }
}
